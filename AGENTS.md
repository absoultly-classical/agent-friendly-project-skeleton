# Agent 协作指南

本项目要求 Agent 仅依靠仓库内的项目文档恢复上下文。聊天记录、个人记忆、
终端历史和外部讨论都不是可靠的信息源；后续工作需要用到的内容，必须沉淀到
项目文档中。

## 首次进入项目

开始工作前，按顺序阅读：

1. [`docs/README.md`](docs/README.md)：项目知识地图与事实来源规则。
2. [`docs/product/overview.md`](docs/product/overview.md)：项目目标、用户和范围。
3. [`docs/architecture/overview.md`](docs/architecture/overview.md)：系统结构与边界。
4. [`docs/development.md`](docs/development.md)：开发约定与验证方式。
5. [`docs/tasks/ACTIVE.md`](docs/tasks/ACTIVE.md)：当前任务和交接状态。
6. 当前任务“必读上下文”中链接的文档与代码。

不要默认读取整个仓库。先沿知识地图和当前任务中的链接逐步展开，只读取完成
当前工作所需的上下文。

## 与用户协作

当用户提出新工作时：

1. 用自己的话复述目标、范围和完成标准，发现歧义时及时确认。
2. 从 `docs/tasks/TEMPLATE.md` 复制任务文档，保存到 `docs/tasks/active/`，并登记到
   `docs/tasks/ACTIVE.md`。
3. 实施前先阅读任务中列出的必读上下文；不要依赖聊天里没有写入文档的旧结论。
4. 工作中发现长期有效的事实、约束或决策时，同步更新对应项目文档。
5. 汇报完成时说明结果、验证证据、未决风险，并更新任务交接区。

用户可以直接说：

- “根据当前项目文档接着做。”
- “为这个需求建立任务记录，然后开始实现。”
- “暂停工作，并把当前状态整理成下一位 Agent 能直接接手的交接记录。”
- “检查项目文档是否足以让一个不了解聊天历史的 Agent 接手。”

## 工作约定

- 实施前必须创建或认领任务，并在 `docs/tasks/ACTIVE.md` 中登记。
- 不要只在聊天、提交说明或代码注释中保存重要背景。
- 长期有效的选择写入 `docs/decisions/`，并说明背景、备选方案和影响。
- 构建、运行和验证方式发生变化时，同步更新 `docs/development.md`。
- 优先执行与改动最相关的验证，并将具体方式和结果写入任务文档。
- 暂停或结束前，更新任务状态、改动范围、验证证据、风险和明确的下一步。
- 项目文档中禁止写入密钥、令牌、个人信息或生产环境凭据。

## 提交与测试

- 每完成一个功能就提交一次 Git。保持“一个功能一次提交”的粒度，便于后续追踪
  改动来源、定位问题，并回滚到任意一个可用状态。
- 提交说明写清这次提交完成了什么功能，并关联对应的任务编号。不要把多个互不
  相关的功能合并进同一次提交，也不要长时间把已完成的功能留在工作区不提交。
- 每次提交前必须完成这次功能的完整测试。涉及 Web 代码时，在 `web/` 目录运行
  `npm run lint` 与 `npm run check`（自动化测试、TypeScript 类型检查、生产构建）。
- 新增或修改功能时同步补齐自动化测试，让这次改动的关键行为具备测试覆盖。
- 测试没有全部通过时，不提交、不交付。测试失败要先修复真实问题，不允许跳过、
  注释或放宽断言来让结果变绿。
- 交付给用户之前，确认最终状态下所有测试均通过，并把执行的命令与结果写入任务
  文档，作为可复核的验证证据。

## 信息冲突时的优先级

1. 当前可执行行为和自动化测试
2. 已采纳的决策记录
3. 产品、架构、开发和运维文档
4. 当前任务文档
5. 代码注释与提交说明
6. 聊天记录

发现冲突时不要静默选择。应核实真实情况，修正文档；暂时无法解决的，记录到
当前任务的“风险与未决问题”中，并注明需要谁确认。

## 完成标准

只有在实现、验证结果和受影响的项目文档彼此一致，并且任务文档已经留下完整
交接信息时，工作才算完成。最终自检标准是：一个不了解任何聊天历史的新 Agent，
能否仅靠仓库内容解释现状并继续工作。

