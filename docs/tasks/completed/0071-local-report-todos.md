# 0071：让当前会话报告待办符合本地范围

## 目标

修正当前本地会话报告仍显示历史课程缺勤提醒和测验错题待办的问题，让课后待办与本地报告能实际完成的操作一致。

## 范围

- 当前本地报告显示导出摘要和复制报告链接待办。
- 导出本地摘要成功后自动完成对应待办。
- 历史会议报告继续显示课程演示待办。
- 增加 jsdom 回归测试，确认当前报告不出现课程缺勤和错题待办。

## 验收标准

- [x] 当前会话显示本地导出和分享待办。
- [x] 当前会话不显示历史课程缺勤提醒和测验错题待办。
- [x] 当前报告导出成功后导出待办自动勾选。
- [x] 历史会议待办保持演示内容。
- [x] test、typecheck、lint、build 通过。

## 验证记录

2026-08-29：当前本地报告改用导出摘要、复制链接两项本地待办，成功导出后自动勾选；历史报告保留课程演示待办。新增回归测试并同步修正旧断言。`npm run check` 通过（2 个测试文件、96 项测试，typecheck 和 build 通过），`npm run lint` 通过，`git diff --check` 通过（仅有换行符提示）。

## 风险与边界

待办状态只存在于当前报告页面生命周期；本地报告不提供真实课程通知、测验分析或服务端任务同步。
