# 0160：回放查看区支持 Escape 关闭

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-006、REQ-010
- 关联决策：无

## 目标

让回放管理页和课后报告页的查看区域都能用 Escape 退出，保持查看层的键盘可达性和关闭行为一致。

## 范围

### 包含

- 为回放管理页选中的回放查看器增加 Escape 关闭。
- 为课后报告页的回放查看器增加 Escape 关闭。
- 为两条路径补充回归测试并更新长期文档。

### 不包含

- 不改变回放内容、分享引用、本地发布标记或报告快照。
- 不引入浏览器自动化测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：项目核心流程使用 Vitest/jsdom 验证，不使用浏览器测试。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计回放管理页和报告页查看区域的关闭路径。
- [x] 增加两个查看器的 Escape 监听和回归测试。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 回放管理页打开查看器后按 Escape 即关闭。
- [x] 课后报告页打开回放查看器后按 Escape 即关闭。
- [x] 本地发布标记、报告快照和显式关闭行为不回归。

## 工作记录

- 2026-08-30：审计发现两个回放查看器仅提供显式关闭按钮，未提供键盘关闭路径。
- 2026-08-30：在各自组件内按打开状态注册并清理 Escape 监听，避免页面切换后残留监听。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 回放管理页 Escape | Vitest/jsdom | 通过；查看器按 Escape 关闭 | 2026-08-30 |
| 课后报告页 Escape | Vitest/jsdom | 通过；查看器按 Escape 关闭 | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；204/204 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现、测试和文档改动已完成，待本轮门禁确认。
- 改动范围：回放管理页、课后报告页、页面测试、需求文档、架构文档和 0160 任务记录。
- 风险与未决问题：未发现超出本地交互原型范围的风险。
- 下一步：完成门禁后继续审计资料详情和其他查看层的键盘行为。
