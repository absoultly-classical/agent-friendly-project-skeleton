# TASK-ID：会议弹窗焦点循环

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-001、REQ-007

## 目标

完善会议弹窗的键盘焦点管理，让 Tab 不会逃逸到遮罩背后的页面，并在关闭后恢复触发按钮焦点。

## 范围

### 包含

- 在弹窗内实现正向和反向 Tab 边界循环。
- 关闭弹窗时恢复打开它的元素焦点。
- 增加 jsdom 键盘焦点回归测试。

### 不包含

- 不引入第三方无障碍库。
- 不进行浏览器自动化或真实辅助技术兼容性测试。

## 计划

- [x] 核对既有弹窗焦点行为和未覆盖边界。
- [x] 增加焦点元素收集与 Tab 边界循环。
- [x] 增加关闭后的焦点恢复测试。
- [x] 执行完整检查并归档任务。

## 验收标准

- [x] 弹窗首元素反向 Tab 会跳到末元素。
- [x] 弹窗末元素正向 Tab 会回到首元素。
- [x] Escape 关闭后焦点回到触发按钮。
- [x] 测试、lint、类型检查、生产构建和 diff 检查通过。

## 工作记录

- 2026-08-29：早期 0018 仅覆盖初始焦点和 Escape，明确未实现完整焦点陷阱与返回焦点策略；当前继续补齐该边界。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 焦点循环回归 | `npm test -- app/page.test.tsx` | 通过：55 tests | 2026-08-29 |
| 完整自动化检查 | `npm run check` | 通过：78 tests、typecheck、build | 2026-08-29 |
| lint | `npm run lint` | 通过 | 2026-08-29 |
| diff 格式检查 | `git diff --check` | 通过（仅有换行符提示） | 2026-08-29 |

## 交接

- 当前状态：已完成并可交接。
- 改动范围：`web/app/page.tsx`、`web/app/page.test.tsx`。
- 风险与未决问题：焦点恢复在触发元素被卸载时由浏览器决定，真实辅助技术仍需单独验证。
- 下一步：继续审计会议控制、资料操作和回放入口的实际行为。
