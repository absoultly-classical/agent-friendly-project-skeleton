# 0149：设备枚举请求最新结果生效

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 关联需求：REQ-013、REQ-017、REQ-019

## 目标

避免旧会话或旧 `devicechange` 触发的 `enumerateDevices()` 异步结果覆盖当前会话的
设备列表和选择状态。

## 必读上下文

- `docs/architecture/overview.md`
- `docs/product/requirements.md`
- `web/app/use-local-webrtc.ts`
- `web/app/use-local-webrtc.test.tsx`

## 计划

- [x] 审计设备枚举调用、设备变化监听和离会/重入生命周期。
- [x] 让设备枚举结果按请求序号最新生效。
- [x] 补充旧枚举结果在离会重入后不会污染当前会话的回归测试。
- [x] 运行门禁、更新架构记录并归档。

## 验收标准

- [x] 旧 `enumerateDevices()` 结果不能覆盖新请求返回的设备列表。
- [x] 旧结果不能清空当前会话仍然有效的设备选择。
- [x] 正常设备变化、加入、设备切换和资源清理行为不回退。
- [x] 测试、类型检查、构建、Lint 和 HTTP 检查通过。

## 工作记录

- 2026-08-30：发现 `refreshDevices` 在异步返回后无请求代次校验；旧加入流程或旧设备变化回调可能晚于新会话写入设备状态。
- 2026-08-30：设备枚举增加请求序号，旧结果返回时丢弃；离会清理同时使未完成枚举失效，并新增重入回归测试。
- 2026-08-30：完成全量门禁，0149 验收通过。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 设备枚举最新结果回归 | Vitest/jsdom | hook 测试 45/45 通过 | 2026-08-30 |
| 全量测试 | `npm test -- --runInBand` | 2 个测试文件、188/188 通过 | 2026-08-30 |
| 类型检查 | `npm run typecheck` | 通过 | 2026-08-30 |
| Lint | `npm run lint` | 通过 | 2026-08-30 |
| 构建 | `npm run build` | 通过 | 2026-08-30 |
| HTTP 检查 | `Invoke-WebRequest http://localhost:3000/` | 200 | 2026-08-30 |
| Diff 检查 | `git diff --check` | 通过 | 2026-08-30 |
