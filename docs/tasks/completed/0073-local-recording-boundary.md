# 0073：明确当前会话录制的本地原型边界

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-003、REQ-008
- 关联决策：无

## 目标

让当前本地会话报告准确表达录制开关状态，不把尚未接入 `MediaRecorder` 或云端存储的原型状态误报为已保存的课堂媒体。

## 范围

### 包含

- 当前本地报告使用本地录制状态文案和会话时长。
- 当前本地回放占位区域说明尚未生成真实媒体文件。
- 历史会议报告继续保留云端回放演示文案和固定演示时长。
- 补充 jsdom 回归测试和架构边界说明。

### 不包含

- 不接入真实 `MediaRecorder`、云盘上传、转码、字幕或章节服务。
- 不改变当前会议中的录制开关交互。

## 必读上下文

- [`docs/product/overview.md`](../../product/overview.md)
- [`docs/architecture/overview.md`](../../architecture/overview.md)
- [`docs/development.md`](../../development.md)
- [`web/app/page.tsx`](../../../web/app/page.tsx)
- [`web/app/page.test.tsx`](../../../web/app/page.test.tsx)

## 约束与假设

- 约束：产品非目标明确排除生产级录制，本地实验只在同源窗口传输媒体。
- 约束：当前 `recording` 状态是页面内原型状态，不代表媒体文件存在。
- 假设：保留可打开的回放占位区域，同时明确其为状态预览，可以保持已有操作连续性；通过自动化测试验证。

## 计划

- [x] 为当前会话回放卡片和查看区域替换为本地状态文案。
- [x] 使用当前会话时长替代固定录制时长，并补充历史报告回归。
- [x] 更新架构说明，运行完整门禁并归档任务。

## 验收标准

- [x] 当前会话开启录制后，报告不出现“保存到课程云盘”。
- [x] 当前会话回放区域说明未生成真实媒体文件，并显示当前会话时长。
- [x] 历史会议仍显示云端回放演示文案和 `01:38:42`。
- [x] test、typecheck、lint、build 通过。

## 工作记录

2026-08-29：审计发现当前快照复用了历史报告的云盘文案与固定回放时长，和架构中“未接入云端录制”的边界不一致。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 当前/历史录制文案和时长边界 | jsdom 页面回归 | 通过；100 项测试通过 | 2026-08-29 |
| 类型、构建和代码规范 | `npm run check`、`npm run lint`、`git diff --check` | 通过；仅有换行符提示 | 2026-08-29 |

## 交接

- 当前状态：已完成；当前报告只展示本地录制状态，改动尚未提交。
- 改动范围：`web/app/page.tsx`、`web/app/page.test.tsx`、`docs/architecture/overview.md`。
- 风险与未决问题：真实录制能力需要独立媒体/转码/存储服务，不能在本任务中伪造完成。
- 下一步：在 `ReportPage` 根据 `reportSnapshot` 切换录制卡片和回放查看器文案。
