# 0165：主导航清理临时浮层状态

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-009
- 关联决策：无

## 目标

保证主导航切换后不会把旧页面的全局搜索或通知浮层带到新页面，避免临时上下文覆盖新页面内容。

## 范围

### 包含

- 主导航切换时关闭全局工具浮层。
- 保留既有回放、资料和会议定位状态清理逻辑。
- 增加主导航切换回归测试并更新长期文档。

### 不包含

- 不改变搜索结果、通知已读状态或页面路由结构。
- 不引入浏览器自动化测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：全局工具浮层是当前页面内的临时状态，不承担跨页面持久化。
- 约束：项目核心流程使用 Vitest/jsdom 验证，不使用浏览器测试。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计主导航与全局工具浮层的状态关系。
- [x] 主导航切换时清理工具浮层并增加回归断言。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 搜索或通知浮层打开时切换主导航，浮层立即关闭。
- [x] 新页面可以正常渲染，旧搜索定位状态不会覆盖新页面。
- [x] 既有全局搜索结果导航和通知行为不回归。

## 工作记录

- 2026-08-30：发现主导航处理器清理了结果定位和个人菜单，但未清理全局工具浮层。
- 2026-08-30：补充 `setUtilityPanel(null)`，让主导航成为临时页面上下文的统一边界。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 主导航清理搜索浮层 | Vitest/jsdom | 通过；导航切换后搜索浮层关闭且新页面正常渲染 | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；209/209 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现、测试和文档改动已完成，待本轮门禁确认。
- 改动范围：主导航处理器、页面测试、需求文档、架构文档和 0165 任务记录。
- 风险与未决问题：未发现超出本地交互原型范围的风险。
- 下一步：完成门禁后继续审计导航、分享引用和本地存储状态边界。
