# TASK-ID：重新加入动作语义

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-003、REQ-013、REQ-019

## 目标

让会中设备告警上的“重新加入”按钮真正完成重新加入，而不是只离开并把恢复动作交给用户再次点击。

## 范围

### 包含

- 使用当前音视频偏好直接重新执行本地 WebRTC `join`。
- 保留当前会话的初始静音策略。
- 更新屏幕共享失败后的页面回归测试。

### 不包含

- 不改变媒体权限请求或失败提示文案。
- 不实现后台自动重连或服务端重试策略。

## 计划

- [x] 审计设备告警按钮的实际动作和文案。
- [x] 将“重新加入”连接到带当前偏好的 join 流程。
- [x] 增加重新加入成功回归测试。
- [x] 执行完整检查并归档任务。

## 验收标准

- [x] 设备告警点击“重新加入”后直接进入 starting/waiting 流程。
- [x] 不需要用户再次点击“开启设备并加入”。
- [x] 当前音频、视频和初始静音偏好会被保留。
- [x] 测试、lint、类型检查、生产构建和 diff 检查通过。

## 工作记录

- 2026-08-29：静态审计发现告警按钮绑定 `call.leave`，与“重新加入”文案不符，恢复流程需要额外一次用户点击。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 重新加入回归 | `npm test -- app/page.test.tsx` | 通过：55 tests | 2026-08-29 |
| 完整自动化检查 | `npm run check` | 通过：78 tests、typecheck、build | 2026-08-29 |
| lint | `npm run lint` | 通过 | 2026-08-29 |
| diff 格式检查 | `git diff --check` | 通过（仅有换行符提示） | 2026-08-29 |

## 交接

- 当前状态：已完成并可交接。
- 改动范围：`web/app/page.tsx`、`web/app/page.test.tsx`。
- 风险与未决问题：浏览器可能再次弹出设备权限或被系统拒绝，应用仍会显示可恢复错误。
- 下一步：继续审计报告、资料和会中操作的占位行为。
