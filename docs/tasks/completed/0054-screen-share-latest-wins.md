# TASK-ID：屏幕共享请求 latest-wins

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-003、REQ-016、REQ-019

## 目标

保护屏幕共享的异步请求生命周期，快速连续点击时只提交最后一次共享请求，并停止过期屏幕轨道。

## 范围

### 包含

- 为屏幕共享请求增加序列号和过期结果检查。
- 离会时使未完成的共享请求失效。
- 增加快速连续共享的 fake MediaStream 回归测试。

### 不包含

- 不改变浏览器屏幕共享权限或采集约束。
- 不实现录制、转码或云端共享服务。

## 计划

- [x] 审计 `getDisplayMedia` 与共享状态的异步竞态。
- [x] 增加屏幕共享请求序列守卫。
- [x] 增加过期屏幕轨道清理测试。
- [x] 执行完整检查并归档任务。

## 验收标准

- [x] 快速连续共享时旧屏幕轨道会停止。
- [x] 只有最后一次共享请求保持 active。
- [x] 离会后返回的共享结果仍会被停止。
- [x] 测试、lint、类型检查、生产构建和 diff 检查通过。

## 工作记录

- 2026-08-29：审计发现共享按钮在 `getDisplayMedia()` 尚未返回时无法区分重复请求，旧屏幕流可能泄漏。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 屏幕共享竞态回归 | `npm test -- app/use-local-webrtc.test.tsx` | 通过：23 tests | 2026-08-29 |
| 完整自动化检查 | `npm run check` | 通过：77 tests、typecheck、build | 2026-08-29 |
| lint | `npm run lint` | 通过 | 2026-08-29 |
| diff 格式检查 | `git diff --check` | 通过（仅有换行符提示） | 2026-08-29 |

## 交接

- 当前状态：已完成并可交接。
- 改动范围：`web/app/use-local-webrtc.ts`、`web/app/use-local-webrtc.test.tsx`。
- 风险与未决问题：本地 fake MediaStream 不替代真实浏览器共享权限与系统窗口行为测试。
- 下一步：继续审计报告、资料和会中按钮的占位行为，优先补齐可验证交互。
