# 0162：会中侧栏支持 Escape 关闭

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-003
- 关联决策：无

## 目标

让会中成员、聊天和课堂互动侧栏可以通过 Escape 关闭，统一会中临时层的键盘退出行为。

## 范围

### 包含

- 将会中侧栏状态纳入页面级 Escape 清理。
- 增加成员侧栏 Escape 回归测试。
- 更新需求和架构文档。

### 不包含

- 不改变侧栏内容、成员控制或聊天信令。
- 不引入浏览器自动化测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：成员和聊天范围仍遵守当前本地 WebRTC 实验边界。
- 约束：项目核心流程使用 Vitest/jsdom 验证，不使用浏览器测试。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计会中侧栏的打开、关闭和全局键盘事件路径。
- [x] 增加 Escape 关闭逻辑和回归测试。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 成员侧栏打开后按 Escape 会关闭，其他侧栏使用同一清理路径。
- [x] 侧栏显式关闭、更多设置和会议控制行为不回归。
- [x] 相关验证和长期文档更新完成。

## 工作记录

- 2026-08-30：发现全局 Escape 已清理搜索、个人菜单和会中设置，但没有清理会中侧栏。
- 2026-08-30：将 `panel` 纳入页面级 Escape 清理，避免各侧栏分别维护重复监听。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 成员侧栏 Escape | Vitest/jsdom | 通过；成员侧栏按 Escape 关闭并恢复 `aria-expanded=false` | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；204/204 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现、测试和文档改动已完成，待本轮门禁确认。
- 改动范围：会中页面、页面测试、需求文档、架构文档和 0162 任务记录。
- 风险与未决问题：未发现超出本地交互原型范围的风险。
- 下一步：完成门禁后继续审计剩余页面状态切换和本地数据边界。
