# TASK-ID：会议弹窗表单提交语义

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-001、REQ-013

## 目标

让创建会议和加入会议弹窗具备标准表单提交行为，支持键盘提交，同时避免关闭和模式切换按钮误触发提交。

## 范围

### 包含

- 将弹窗内容改为带 `onSubmit` 的表单。
- 明确关闭、课程/普通会议切换按钮为非提交按钮。
- 增加弹窗提交回归测试。

### 不包含

- 不改变会议字段校验规则或提交后的业务逻辑。
- 不引入新的表单库。

## 计划

- [x] 审计弹窗 DOM 结构和现有按钮类型。
- [x] 增加表单提交处理并标注非提交按钮。
- [x] 增加创建/加入弹窗回归测试。
- [x] 执行完整检查并归档任务。

## 验收标准

- [x] 加入弹窗触发表单提交后进入本地会议页。
- [x] 创建弹窗提交后进入会议或保存预约。
- [x] 模式切换与关闭按钮不会触发提交。
- [x] 测试、lint、类型检查、生产构建和 diff 检查通过。

## 工作记录

- 2026-08-29：审计发现弹窗使用普通 `section`，输入框没有表单提交语义，键盘 Enter 无法完成主要操作。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 弹窗表单回归 | `npm test -- app/page.test.tsx` | 通过：54 tests | 2026-08-29 |
| 完整自动化检查 | `npm run check` | 通过：75 tests、typecheck、build | 2026-08-29 |
| lint | `npm run lint` | 通过 | 2026-08-29 |
| diff 格式检查 | `git diff --check` | 通过（仅有换行符提示） | 2026-08-29 |

## 交接

- 当前状态：已完成并可交接。
- 改动范围：`web/app/page.tsx`、`web/app/page.test.tsx`。
- 风险与未决问题：表单仍是本地原型交互，不代表已接入服务端创建接口。
- 下一步：继续审计会中操作入口和真实媒体状态的一致性。
