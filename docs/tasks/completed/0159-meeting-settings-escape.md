# 0159：会中设置菜单支持 Escape 关闭

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-003
- 关联决策：无

## 目标

让会中“更多设置”菜单遵循统一的 Escape 关闭行为，避免用户打开设置后只能再次点击菜单按钮才能收起。

## 范围

### 包含

- 将会中设置菜单纳入页面级 Escape 状态清理。
- 增加设置菜单 Escape 回归测试。
- 更新需求和架构文档。

### 不包含

- 不改变菜单内的实验能力说明或会议设置项。
- 不引入浏览器自动化测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：项目核心流程使用 Vitest/jsdom 验证，不使用浏览器测试。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计会中设置菜单的打开、关闭和全局键盘事件路径。
- [x] 增加 Escape 关闭逻辑和回归测试。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 会中设置菜单打开后按 Escape 即关闭，按钮 `aria-expanded` 恢复为 `false`。
- [x] 现有会议菜单和其他键盘关闭行为不回归。
- [x] 相关验证和长期文档更新完成。

## 工作记录

- 2026-08-30：发现页面级 Escape 仅清理通用工具面板和个人菜单，未清理会中设置菜单。
- 2026-08-30：补充 `setMeetingSettingsOpen(false)`，并加入打开后按 Escape 的回归断言。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 设置菜单 Escape 行为 | Vitest/jsdom | 通过，菜单消失且 `aria-expanded=false` | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；204/204 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现和回归测试已完成，待本轮门禁确认。
- 改动范围：会中页面、页面测试、需求文档、架构文档和 0159 任务记录。
- 风险与未决问题：未发现超出本地交互原型范围的风险。
- 下一步：运行全量门禁并继续审计下一处可验证的产品边界。
