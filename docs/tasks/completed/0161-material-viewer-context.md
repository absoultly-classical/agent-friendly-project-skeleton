# 0161：资料详情与预览上下文一致

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-011
- 关联决策：无

## 目标

保证资料详情和预览始终指向同一份资料，避免切换资料后旧预览残留；同时补齐资料查看链路的键盘退出行为。

## 范围

### 包含

- 选择新资料时清理旧预览。
- 关闭资料详情时同步清理预览。
- Escape 先关闭预览，再关闭详情。
- 补充资料查看回归测试并更新长期文档。

### 不包含

- 不改变静态资料目录、上传元数据或真实文件读取边界。
- 不引入浏览器自动化测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：资料元数据只保存在当前页面，静态演示资料保持只读。
- 约束：项目核心流程使用 Vitest/jsdom 验证，不使用浏览器测试。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计资料详情、预览和选择切换的状态关系。
- [x] 统一资料选择和关闭清理逻辑，增加 Escape 层级关闭。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 选择新资料后不会保留旧资料预览。
- [x] 关闭资料详情会同步清理预览。
- [x] Escape 按预览、详情顺序关闭，现有显式关闭和资料移除行为不回归。

## 工作记录

- 2026-08-30：发现资料选择状态和预览状态可分别保留，切换资料时可能出现详情与预览不一致。
- 2026-08-30：引入统一资料选择入口，并按预览优先的顺序处理 Escape；移除资料时无条件清理查看状态。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 资料查看上下文与 Escape | Vitest/jsdom | 通过；155/155 页面测试通过，覆盖上下文切换和 Escape 关闭顺序 | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；204/204 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现、测试和文档改动已完成，待本轮门禁确认。
- 改动范围：资料页面、页面测试、需求文档、架构文档和 0161 任务记录。
- 风险与未决问题：未发现超出本地交互原型范围的风险。
- 下一步：完成门禁后继续审计页面状态切换和本地数据边界。
