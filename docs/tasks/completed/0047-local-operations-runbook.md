# TASK-ID：本地运行手册完善

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-29
- 更新日期：2026-08-29
- 关联需求：REQ-008、REQ-019
- 关联决策：ADR-0001

## 目标

把运行手册从占位模板补成可交接的本地开发与验证说明，让后续 Agent 或开发者能独立启动、检查和诊断项目，同时理解本地 WebRTC 实验的边界。

## 范围

### 包含

- 记录 `web/` 的依赖安装、开发启动、生产构建和自动化验证命令。
- 记录本地服务健康检查、常见端口/权限/测试故障和安全恢复方式。
- 记录预约数据仅存于本机 `localStorage`、不保存凭据的边界。

### 不包含

- 不新增部署平台、生产环境或凭据配置。
- 不把本地开发服务器描述为生产服务。

## 必读上下文

- `README.md`
- `docs/development.md`
- `docs/architecture/overview.md`
- `docs/operations/runbook.md`
- `web/package.json`

## 约束与假设

- 约束：手册中的命令必须与当前 `web/package.json` 脚本一致。
- 约束：不记录私有地址、凭据或用户数据。
- 假设：当前主要交接对象是本地开发者和后续编码 Agent。

## 计划

- [x] 补充本地环境、验证和健康检查说明。
- [x] 补充常见故障、恢复和数据安全边界。
- [x] 执行文档命令/内容一致性检查并归档任务。

## 验收标准

- [x] 新开发者能从手册找到启动、测试、类型检查、lint 和构建命令。
- [x] 手册明确说明不能使用浏览器自动化作为当前验证要求，并说明 fake WebRTC 测试边界。
- [x] 手册包含端口、权限、存储异常等常见故障的安全处理方式。
- [x] 测试、lint、类型检查、生产构建和 diff 检查通过。

## 工作记录

- 2026-08-29：审计发现 `docs/operations/runbook.md` 仍为占位内容，无法支持当前项目的本地交接和故障诊断。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| package scripts 与运行手册一致性 | 人工核对 `web/package.json` 与 `docs/operations/runbook.md` | 通过 | 2026-08-29 |
| 完整自动化检查 | `cd web; npm run check` | 通过：68 tests、typecheck、build | 2026-08-29 |
| lint | `cd web; npm run lint` | 通过 | 2026-08-29 |
| diff 格式检查 | `git diff --check` | 通过 | 2026-08-29 |

## 交接

- 当前状态：已完成并可交接。
- 改动范围：`docs/operations/runbook.md` 及本任务文档。
- 风险与未决问题：手册完善不能替代生产部署、监控和告警系统。
- 下一步：继续进行本地代码与产品行为审计，发现明确缺口后创建下一项任务。
