# 0167：补齐临时层的可访问性关系

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 更新日期：2026-08-30
- 关联需求：REQ-003、REQ-009、REQ-011
- 关联决策：无

## 目标

让搜索、通知、会中设置和会中侧栏的触发按钮向辅助技术明确表达展开状态、关联区域和弹出关系。

## 范围

### 包含

- 为全局搜索和通知按钮增加展开状态、关联区域和弹出关系。
- 为会中设置菜单和会中侧栏补充区域语义与控制关联。
- 增加属性同步回归断言并更新长期文档。

### 不包含

- 不改变视觉样式、页面布局或业务状态。
- 不引入浏览器自动化或屏幕阅读器运行时测试。

## 必读上下文

- `web/app/page.tsx`
- `web/app/page.test.tsx`
- `docs/product/requirements.md`
- `docs/architecture/overview.md`

## 约束与假设

- 约束：当前项目的可访问性回归使用 Testing Library/jsdom，不能替代真实辅助技术验收。
- 约束：临时区域仍为当前页面状态，不改变本地原型边界。
- 约束：保留工作区中已有未提交改动，不清理或回滚无关文件。

## 计划

- [x] 审计临时层触发器和内容区域的语义关联。
- [x] 补充 ARIA 属性和对应区域标识。
- [x] 运行测试、类型检查、Lint、构建和 HTTP 检查，并更新长期文档。

## 验收标准

- [x] 搜索/通知按钮的 `aria-expanded` 随浮层开关同步，并指向对应区域。
- [x] 会中设置菜单和会中侧栏具有可识别的控制关系与区域语义。
- [x] 既有临时层交互和键盘关闭行为不回归。

## 工作记录

- 2026-08-30：发现多个临时层只依赖视觉状态，触发器没有把展开关系暴露给辅助技术。
- 2026-08-30：为全局工具浮层、会中设置菜单和侧栏补齐最小必要 ARIA 语义。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| ARIA 状态与关联属性 | Vitest/jsdom | 通过；搜索/通知展开状态与关联区域属性同步，设置菜单和侧栏语义可查询 | 2026-08-30 |
| 全量测试、类型、Lint、构建 | npm scripts | 通过；209/209 测试、类型检查、Lint 和构建通过（含 vinext 动态路由分类提示） | 2026-08-30 |
| HTTP 检查 | PowerShell `Invoke-WebRequest` | 通过；HTTP 200 | 2026-08-30 |

## 交接

- 当前状态：实现、测试和文档改动已完成，待本轮门禁确认。
- 改动范围：页面临时层、页面测试、需求文档、架构文档和 0167 任务记录。
- 风险与未决问题：真实辅助技术仍需在后续设备验收中确认。
- 下一步：完成门禁后继续审计表单、分享引用和本地会话生命周期。
