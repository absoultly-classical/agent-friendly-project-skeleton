# 0150：拒绝带非法房间号的预约记录

- 状态：已完成
- 负责人：Codex
- 创建日期：2026-08-30
- 关联需求：REQ-001、REQ-009、REQ-012

## 目标

本地预约记录仅在完全缺少 `roomId` 时兼容默认房间号；如果记录明确包含非法或超范围
房间号，应整条忽略，不能伪装成默认预约。

## 必读上下文

- `docs/tasks/completed/0145-stored-room-id-boundary.md`
- `docs/architecture/overview.md`
- `web/app/page.tsx`
- `web/app/page.test.tsx`

## 计划

- [x] 复核 0145 的预约兼容逻辑和测试等待条件。
- [x] 区分缺少房间号与存在非法房间号两种情况。
- [x] 强化超长预约房间号测试，确认异步初始化完成后仍被忽略。
- [x] 运行门禁、更新架构记录并归档。

## 验收标准

- [x] 缺少 `roomId` 的合法旧预约仍使用兼容默认房间号。
- [x] 明确存在的非法、超长房间号预约不会显示或覆盖默认预约。
- [x] 本地历史房间号边界、搜索和正常预约流程不回退。
- [x] 测试、类型检查、构建、Lint 和 HTTP 检查通过。

## 工作记录

- 2026-08-30：复核发现 `readScheduledMeeting` 会把存在但非法的 `roomId` 与缺失字段都回退到默认房间；0145 新增测试还可能在初始化定时器执行前提前通过。
- 2026-08-30：仅对完全缺失的 `roomId` 使用兼容默认值，明确存在的非法房间号整条预约忽略；强化异步初始化后的边界测试。
- 2026-08-30：完成全量门禁，0150 验收通过。

## 验证证据

| 验证内容 | 方式 | 结果 | 日期 |
| --- | --- | --- | --- |
| 非法预约房间号回归 | Vitest/jsdom | 页面测试 143/143 通过 | 2026-08-30 |
| 全量测试 | `npm test -- --runInBand` | 2 个测试文件、188/188 通过 | 2026-08-30 |
| 类型检查 | `npm run typecheck` | 通过 | 2026-08-30 |
| Lint | `npm run lint` | 通过 | 2026-08-30 |
| 构建 | `npm run build` | 通过 | 2026-08-30 |
| HTTP 检查 | `Invoke-WebRequest http://localhost:3000/` | 200 | 2026-08-30 |
| Diff 检查 | `git diff --check` | 通过 | 2026-08-30 |
