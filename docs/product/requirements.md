# 产品需求

每条需求使用稳定编号，方便任务、测试和决策记录引用，而不必到处复制全文。

| 编号 | 需求 | 验收证据 | 状态 |
| --- | --- | --- | --- |
| REQ-001 | 替换为第一项用户可观察的行为。 | 写明可以证明它的测试、指标或评审结果。 | 待确认 |

## 状态约定

需求状态使用：`待确认`、`已确认`、`已实现`、`已废弃`、`已拒绝`。

已经确认的编号不要删除；需求失效时应保留记录，修改状态，并链接解释原因的决策
记录。安全性、可用性、性能、无障碍、隐私和兼容性等非功能要求，如果会约束系统
的可观察行为，也应在这里登记。

