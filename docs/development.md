# 开发指南

## 项目结构

- `web/`：学习通会议 Web 原型。
- `docs/`：产品、架构、任务和长期决策。

## 开发环境

Web 原型使用 React、TypeScript、Vinext 和 Tailwind CSS。依赖清单与具体运行方式以
`web/package.json` 为准，Agent 应在 `web/` 目录完成开发与验证。

## 本地运行

Agent 启动开发环境后，应先确认首页能够正常响应，再向用户打开预览。用户不需要
执行命令或维护本地环境。

## 验证方式

- 自动化回归：`web/package.json` 的 `test` 脚本在 jsdom 中使用可控模拟对象验证本地
  WebRTC 状态机和会议页面关键流程，不需要真实浏览器、摄像头、麦克风或人工授权。
- 代码规范：`lint` 脚本执行 ESLint 全量检查，提交 Web 改动前应确保通过。
- 完整检查：`check` 脚本依次执行自动化测试、TypeScript 类型检查和生产构建；Agent
  在交付涉及 Web 代码的改动前应运行该脚本。
- 构建验证：生产构建能够成功产出部署文件。
- 交互验证：首页、创建会议、会中控制台、教学互动和课后报告均可进入。
- 响应式验证：桌面和手机宽度下不存在阻塞主要操作的溢出或遮挡；窄屏会中底部控制区保留全部核心操作，空间不足时只在控制组内部横向滚动，结束会议按钮保持可达；长会议标题在顶部收缩显示。
- 可访问性验证：交互元素具有清晰名称，键盘焦点可见，状态不只依赖颜色表达。

## 提交粒度

每完成一个功能提交一次 Git，提交说明写清完成的功能并关联任务编号。提交前这次
功能的完整测试必须全部通过；测试失败时先修复真实问题，不通过跳过或放宽断言的
方式让结果变绿。详细约定见 [`AGENTS.md`](../AGENTS.md) 的“提交与测试”。

## 门禁自动化

这两条纪律由两层机械门禁保证，不只依赖执行者自觉：

- 远端门禁：`.github/workflows/ci.yml` 在每次 push 与 pull request 上安装依赖并执行
  `npm run lint` 与 `npm run check`。任一步失败即判定该提交不合格。
- 本地门禁：`.githooks/pre-commit` 在提交前执行同样两步。仅当暂存区包含 `web/`
  改动时运行，纯文档提交自动跳过。需要在仓库根目录启用一次：

  ```sh
  git config core.hooksPath .githooks
  ```

  启用后若缺少 `web/node_modules`，钩子会提示先执行 `npm ci` 并中止提交。

本地钩子是选择启用而非自动安装：Git 不会随克隆自动启用钩子，仓库也不擅自改写
使用者的 Git 配置。远端 CI 不依赖本地是否启用钩子，始终生效。

## 每次改动的检查清单

1. 当前任务已链接相关需求和决策编号。
2. 预期实现符合架构边界和依赖方向。
3. 主要用户流程有明确的交互反馈。
4. 已运行 lint 和完整检查脚本，并记录自动化测试、类型检查和生产构建结果。
5. 本次改动的关键行为已有自动化测试覆盖，且全部测试通过。
6. 产品文档、任务交接和实际界面彼此一致。
7. 功能完成后已单独提交 Git，工作区没有残留未提交的已完成功能。
